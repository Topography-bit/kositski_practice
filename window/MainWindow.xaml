<Window x:Class="WpfOrderCalculator.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Калькулятор заказа"
        Height="320"
        Width="460"
        MinHeight="300"
        MinWidth="420"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="Имя клиента"
                   FontSize="15"
                   FontWeight="SemiBold" />

        <TextBox x:Name="ClientNameTextBox"
                 Grid.Row="1"
                 Margin="0,8,0,14"
                 Height="34"
                 FontSize="15"
                 VerticalContentAlignment="Center" />

        <Button Grid.Row="2"
                Content="Оформить заказ"
                Width="170"
                Height="36"
                HorizontalAlignment="Left"
                Click="OpenOrderButton_Click" />

        <Border Grid.Row="3"
                Margin="0,24,0,0"
                Padding="14"
                BorderBrush="#D0D7DE"
                BorderThickness="1"
                Background="#F6F8FA"
                CornerRadius="6">
            <TextBlock x:Name="ResultLabel"
                       Text="Итог появится после оформления заказа."
                       FontSize="15"
                       TextWrapping="Wrap" />
        </Border>
    </Grid>
</Window>
