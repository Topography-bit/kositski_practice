<Window x:Class="TaskPlanner.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:TaskPlanner.ViewModels"
        xmlns:conv="clr-namespace:TaskPlanner.Converters"
        Title="Планировщик задач" Height="620" Width="820" Background="#1E1E2E">
    <Window.DataContext><vm:MainViewModel/></Window.DataContext>
    <Window.Resources><conv:DeadlineColorConverter x:Key="DColor"/></Window.Resources>
    <Grid Margin="20">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
        <TextBlock Text="{Binding CurrentTime}" FontSize="18" FontWeight="SemiBold" Foreground="#CDD6F4" HorizontalAlignment="Center" Margin="0,0,0,16"/>
        <Border Grid.Row="1" Background="#313244" CornerRadius="8" Padding="16" Margin="0,0,0,16">
            <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="180"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                <TextBox Text="{Binding NewTaskTitle, UpdateSourceTrigger=PropertyChanged}" Background="#45475A" Foreground="#CDD6F4" Padding="8,6" FontSize="14" Margin="0,0,8,0" VerticalContentAlignment="Center"/>
                <DatePicker Grid.Column="1" SelectedDate="{Binding NewTaskDeadline}" Margin="0,0,8,0"/>
                <Button Grid.Column="2" Content="+ Добавить" Command="{Binding AddTaskCommand}" Background="#89B4FA" Foreground="#1E1E2E" FontWeight="Bold" Padding="16,6" BorderThickness="0"/>
            </Grid>
        </Border>
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Tasks}"><ItemsControl.ItemTemplate><DataTemplate>
                <Border Background="#313244" CornerRadius="6" Margin="0,0,0,8" Padding="14,10">
                    <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="200"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="{Binding Title}" Foreground="#CDD6F4" FontSize="15" FontWeight="Medium"/>
                            <TextBlock Text="{Binding DeadlineFormatted}" Foreground="#585B70" FontSize="12"/>
                        </StackPanel>
                        <TextBlock Grid.Column="1" Text="{Binding TimeLeftFormatted}" Foreground="{Binding IsOverdue, Converter={StaticResource DColor}}" FontSize="13" FontWeight="SemiBold" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,12,0"/>
                        <Button Grid.Column="2" Content="Готово" Command="{Binding DataContext.CompleteTaskCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}" CommandParameter="{Binding}" Background="#A6E3A1" Foreground="#1E1E2E" FontSize="12" FontWeight="Bold" Padding="10,4" BorderThickness="0" Margin="0,0,6,0"/>
                        <Button Grid.Column="3" Content="X" Command="{Binding DataContext.RemoveTaskCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}" CommandParameter="{Binding}" Background="#F38BA8" Foreground="#1E1E2E" FontSize="12" FontWeight="Bold" Padding="8,4" BorderThickness="0"/>
                    </Grid>
                </Border>
            </DataTemplate></ItemsControl.ItemTemplate></ItemsControl>
        </ScrollViewer>
    </Grid>
</Window>
