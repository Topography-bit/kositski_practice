<Window x:Class="CurrencyConverterApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Конвертер валют"
        Height="430"
        Width="520"
        MinHeight="400"
        MinWidth="480"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="Конвертер валют REST API"
                   FontSize="22"
                   FontWeight="SemiBold"
                   Margin="0,0,0,18" />

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="16" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <StackPanel>
                <TextBlock Text="Из валюты" FontWeight="SemiBold" />
                <ComboBox x:Name="BaseCurrencyComboBox" Height="34" Margin="0,6,0,0" />
            </StackPanel>

            <StackPanel Grid.Column="2">
                <TextBlock Text="В валюту" FontWeight="SemiBold" />
                <ComboBox x:Name="TargetCurrencyComboBox" Height="34" Margin="0,6,0,0" />
            </StackPanel>
        </Grid>

        <StackPanel Grid.Row="2" Margin="0,16,0,0">
            <TextBlock Text="Сумма" FontWeight="SemiBold" />
            <TextBox x:Name="AmountTextBox"
                     Height="34"
                     Margin="0,6,0,0"
                     VerticalContentAlignment="Center"
                     Text="100" />
        </StackPanel>

        <Button Grid.Row="3"
                Width="150"
                Height="36"
                Margin="0,20,0,0"
                HorizontalAlignment="Left"
                Content="Конвертировать"
                Click="ConvertButton_Click" />

        <Border Grid.Row="4"
                Margin="0,24,0,0"
                Padding="16"
                Background="#F6F8FA"
                BorderBrush="#D0D7DE"
                BorderThickness="1"
                CornerRadius="6">
            <StackPanel>
                <TextBlock x:Name="ResultLabel"
                           FontSize="20"
                           FontWeight="SemiBold"
                           Text="Результат появится здесь" />
                <TextBlock x:Name="StatusLabel"
                           Margin="0,8,0,0"
                           Foreground="#57606A"
                           TextWrapping="Wrap" />
            </StackPanel>
        </Border>
    </Grid>
</Window>
